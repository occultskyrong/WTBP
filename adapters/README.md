# 跨模型适配

此目录存放从 WTBP 规范源生成的适配产物或生成器。规范源始终是 `ontology/`、`practices/`、`skills/` 与 `registry/`；不要在不同 Agent 的规则文件中手工复制知识。

建议投影：

- `AGENTS.md`：项目事实、硬约束和 WTBP 入口。
- Agent Skills：可按需触发的执行流程。
- 工具专有规则：仅承载无法由通用规则表达的格式差异。
