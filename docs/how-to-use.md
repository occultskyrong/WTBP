# 使用方式

## 作为人类知识库

从目录中找到与问题最接近的 Practice，先核对适用场景和不适用场景，再比较候选方案。将最终选择写入项目自己的 Decision Record。

## 作为 Agent 上下文

项目规则文件只应链接到 WTBP 和项目事实。Agent 在遇到高影响、缺少内部模式或需要比较替代方案的问题时，调用 `practice-search`，再按需加载对应 Practice、证据和参考实现。

## 作为自动化能力

只有流程稳定且可重复的部分才沉淀为 Skill。Skill 应先收集上下文，再引用 Practice，最后输出建议和验证结果。
