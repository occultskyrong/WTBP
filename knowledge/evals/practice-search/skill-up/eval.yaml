schema_version: v1alpha1

environment:
  type: none

skills:
  - source: local_path
    path: .
    include:
      - SKILL.md
      - references/**

engine:
  name: claude_code

cases:
  files:
    - evals/cases/positive-architecture-decision.yaml
    - evals/cases/negative-translation.yaml
    - evals/cases/boundary-missing-context.yaml
    - evals/cases/adversarial-stale-practice.yaml
  parallelism: 1

benchmark:
  enabled: true

report:
  formats: [json]
  artifacts: [transcript]
