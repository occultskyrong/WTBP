schema_version: v1alpha1

environment:
  type: none

skills:
  - source: local_path
    path: ../../../skills/skill-evaluation
    include:
      - SKILL.md

engine:
  name: claude_code

cases:
  files:
    - skill-up/cases/positive-full-review.yaml
    - skill-up/cases/negative-simple-edit.yaml
    - skill-up/cases/boundary-missing-eval.yaml
    - skill-up/cases/adversarial-auto-modify.yaml
  parallelism: 1

benchmark:
  enabled: true

report:
  formats: [json]
  artifacts: [transcript]
