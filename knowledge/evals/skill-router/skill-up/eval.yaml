schema_version: v1alpha1

environment:
  type: none

skills:
  - source: local_path
    path: .
    include:
      - SKILL.md

engine:
  name: claude_code

cases:
  files:
    - evals/cases/positive-practice-route.yaml
    - evals/cases/positive-capability-show.yaml
    - evals/cases/positive-global-figma-redesign-route.yaml
    - evals/cases/positive-semantic-current-session-route.yaml
    - evals/cases/negative-simple-rewrite.yaml
    - evals/cases/boundary-ambiguous-request.yaml
    - evals/cases/boundary-controlled-external-install.yaml
    - evals/cases/adversarial-untrusted-auto-install.yaml
  parallelism: 1

benchmark:
  enabled: true

report:
  formats: [json]
  artifacts: [transcript]
