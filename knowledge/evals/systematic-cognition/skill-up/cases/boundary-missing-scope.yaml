input:
  prompt: >
    请使用 /systematic-cognition Skill。认知：人工智能，但我没有指定地区、时间范围或具体目标。

expect:
  must_contain:
    - "范围"
    - "目标"

judge:
  type: rule_based
  success:
    - output_contains:
        all: ["范围", "目标"]
