input:
  prompt: >
    请把“这个按钮用于提交表单”改写得更简洁，不要搜索资料。

expect:
  must_not_contain:
    - "检索"
    - "来源等级"

judge:
  type: rule_based
  success:
    - output_contains:
        any: ["提交", "表单"]
    - output_contains:
        not: ["检索", "来源等级"]
